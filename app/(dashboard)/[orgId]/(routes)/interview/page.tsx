import CandidateView from '@/components/interview/candidates-view';
import { SectionWrap } from '@/components/share';
import React from 'react';

const page = () => {
  return (
    <SectionWrap>
      <CandidateView />
    </SectionWrap>
  );
};

export default page;
